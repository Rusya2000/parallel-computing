#include<iostream>
#include <math.h>
using namespace std;

int main() {
	ro = 7800;
	h = 0.0001; // шаг по х
	l = 46; // coef teploprovodnosti
	c = 460; // удельная теплоемкость
	U[n]
	d = 0.6;
	A = l / pow(h, 2);
		for (int i = 0; i < 60; i + d) {
			for (int j = 0; j < n; j++) {
				alpha[j] = A / c - b
				for (int s = 0; s < n; s++) {

				}
			}
	}
}